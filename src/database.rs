/**
 * Database State neccesary for rocket, wrap db connection inside of it or whatever you want.
 */
pub struct Database {}

impl Database {
    pub fn new() -> Database {
        Database {}
    }
}
